services:
  php:
    container_name: php
    build:
      context: .
    depends_on:
      - db
      - mailer
    ports:
      - "9000:80"
    volumes:
      - .:/var/www/html
      - vendor_data:/var/www/html/app/vendor
      - var_data:/var/www/html/app/var

  db:
    image: postgres:16-alpine
    container_name: db
    environment:
      POSTGRES_DB: app
      POSTGRES_USER: app
      POSTGRES_PASSWORD: app
    ports:
      - "5433:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

  adminer:
    image: adminer:5.4.2
    container_name: adminer
    depends_on:
      - db
    ports:
      - "8080:8080"

  mailer:
    image: axllent/mailpit
    container_name: mailer
    ports:
      - "8025:8025"
    environment:
      MP_SMTP_AUTH_ACCEPT_ANY: 1
      MP_SMTP_AUTH_ALLOW_INSECURE: 1

volumes:
  db_data:
  vendor_data:
  var_data:

